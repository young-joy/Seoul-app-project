<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/loading_dialog_content"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:focusable="true"
    android:focusableInTouchMode="true"
    android:gravity="center">

    <LinearLayout
        android:layout_width="@dimen/loading_diameter"
        android:layout_height="@dimen/loading_diameter"
        android:background="@drawable/bg_circle"
        android:gravity="center"
        android:orientation="horizontal">

        <ImageView
            android:id="@+id/loading_dialog_img_view"
            android:layout_width="@dimen/loading_icon"
            android:layout_height="@dimen/loading_icon" />
    </LinearLayout>

</LinearLayout>